var prev = [];

var addDot = function(e) {
  x = e.offsetX;
  y = e.offsetY;
  circ=createElementNS(“http://www.w3.org/2000/svg”, "circle");
  circ.setAttribute("r", "25");
  s.appendChild(c);
};

xd.addEventListener("click", addDot);
